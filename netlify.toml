[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NEXT_TELEMETRY_DISABLED = "1"

# Force Next.js middleware to run as serverless (Node.js), not edge
[functions]
  node_bundler = "nft"

[[plugins]]
  package = "@netlify/plugin-nextjs"
